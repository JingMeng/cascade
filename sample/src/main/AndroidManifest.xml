<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    tools:ignore="AllowBackup">

    <activity
      android:name=".SampleActivity"
      android:exported="true"
      android:label="Cascade"
      android:taskAffinity="me.saket.cascade">

    </activity>

    <activity
      android:name=".ComposeSampleActivity"
      android:exported="true"
      android:label="Cascade (compose)"
      android:taskAffinity="me.saket.cascade.compose">

    </activity>
    <activity
      android:name=".vectormenu.screen.VectorMenuProviderSampleActivity"
      android:exported="true"
      android:label="@string/vector_menu_activity_title">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

  </application>
</manifest>
